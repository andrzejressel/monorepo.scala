shared:
  my_ci: &common_checks
    - check-success="Code checks"
    - check-success="Scalops build (11, ubuntu-latest)"
    - check-success="Scalops build (17, ubuntu-latest)"

queue_rules:
  - name: default
    conditions: *common_checks

pull_request_rules:
  - name: automatic merge for scala-steward PRs
    conditions:
      - author=4ndrz3j-r35531[bot]
    actions:
      queue:
        name: default
  - name: automatic merge for main when CI passes
    conditions:
      - base=main
      - label=ready-to-merge
      - and: *common_checks
    actions:
      queue:
        name: default
  - name: delete head branch after merge
    conditions:
      - merged
    actions:
      delete_head_branch: